web:
  build: docker/containers/apache
  volumes:
    - "./dev/public:/var/www/public"
    - "./cut:/var/www/cut"
  links:
    - "database:database"
  environment:
    - "VIRTUAL_HOST=host.dok"

database:
  image: mariadb
  environment:
    - "MYSQL_ROOT_PASSWORD=docker"
    - "MYSQL_DATABASE=docker_dev"
    - "MYSQL_USER=docker"
    - "MYSQL_PASSWORD=docker"
  ports:
    - "3306"
